import PokeCard from './PokeCard'

const PokeCardList = () => {
  return (
    <section className='container grid grid-cols-1 gap-2 mt-10'>
      <PokeCard />
    </section>
  )
}

export default PokeCardList
